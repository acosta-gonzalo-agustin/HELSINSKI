sequenceDiagram
    participant browser
    participant server

    Note over browser: User writes the note and clicks "Save"
    Note right of browser: The JS code adds the note to the local list and rerenders the UI

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: The server saves the JSON data
    server-->>browser: HTTP status code 201 (Created)
    deactivate server